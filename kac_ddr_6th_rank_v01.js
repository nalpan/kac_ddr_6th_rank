javascript:(function(){rows = document.querySelectorAll("table.normal-table>tbody>tr");first_scores = [];first_cols = rows[0].getElementsByClassName("score");for(col of first_cols){first_scores.push(Number(col.textContent.replace(",", "")));}for(row of rows){cols = row.getElementsByClassName("score");for(i = 0; i<7; i++){score = cols[i].textContent.replace(",", "");sub = Number(score)-first_scores[i];cols[i].innerHTML = cols[i].textContent+'<br>(<span style="color:red">'+sub+'</span>)';}}})()